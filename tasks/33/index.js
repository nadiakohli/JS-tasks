// 33. Given an array of numbers. Find out how many elements from
// the end of the array must be added to make the total more than 10.
// If array in the sum 10 or less return message 'All elements in the sum are equal {count}'

const getCountElements = () => {};

export default getCountElements;
