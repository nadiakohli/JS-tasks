// 3. From the array numbers to deduce a new array in which to replace
// positive values on 1, negative values on -1, zero values leave

const getReplacedArray = (array) => {
  // your code
};

export default getReplacedArray;
